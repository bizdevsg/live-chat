vars:pre-request {
  basePath: /api/v1
}

docs {
  Bruno collection for the CRM-facing SolidChat export endpoints — lookup by handling agent's
  email. See docs/crm-integration.md for the full contract and background.
}
